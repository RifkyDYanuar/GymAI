<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="12dp"
    android:layout_marginBottom="8dp"
    android:gravity="center_vertical">

    <!-- Colored dot indicator -->
    <View
        android:id="@+id/v_muscle_dot"
        android:layout_width="10dp"
        android:layout_height="10dp"
        android:background="@drawable/bg_muscle_dot_primary" />

    <TextView
        android:id="@+id/tv_muscle_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="14dp"
        android:text="Quadriceps (paha depan)"
        android:textColor="@color/on_surface"
        android:textSize="14sp"
        android:fontFamily="@font/poppins" />

    <TextView
        android:id="@+id/tv_muscle_type"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Primer"
        android:textColor="@color/white"
        android:textSize="11sp"
        android:fontFamily="@font/poppins"
        android:paddingHorizontal="10dp"
        android:paddingVertical="4dp"
        android:background="@drawable/bg_muscle_type_badge" />
</LinearLayout>
