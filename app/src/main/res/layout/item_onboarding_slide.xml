<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Full-screen background image (onboarding1 / onboarding2 / onboarding3 via adapter) -->
    <ImageView
        android:id="@+id/iv_onboarding_bg"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:contentDescription="Onboarding background" />

    <!-- Title & subtitle overlay — teks tetap ada di atas gambar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:paddingStart="28dp"
        android:paddingEnd="28dp"
        android:paddingBottom="100dp">

        <TextView
            android:id="@+id/tv_onboarding_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@android:color/white"
            android:textSize="26sp"
            android:textStyle="bold"
            android:fontFamily="@font/poppins"
            android:lineSpacingExtra="4dp" />

        <TextView
            android:id="@+id/tv_onboarding_subtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:textColor="#CCffffff"
            android:textSize="14sp"
            android:fontFamily="@font/poppins"
            android:lineSpacingExtra="2dp" />

    </LinearLayout>

</FrameLayout>
